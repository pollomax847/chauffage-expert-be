{
    "framework": "nextjs",
    "buildCommand": "./build_web.sh",
    "outputDirectory": "build/web",
    "installCommand": "flutter pub get",
    "routes": [
        { "handle": "filesystem" },
        { "src": "/(.*)", "dest": "/" }
    ]
}